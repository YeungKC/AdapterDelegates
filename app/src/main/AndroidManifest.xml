<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.yeungkc.itemdelegate.sample"
    >

  <application
      android:allowBackup="false"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:theme="@style/AppTheme"
      >
    <activity
        android:name=".activity.MainActivity"
        android:label="@string/app_name"
        >
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity android:name=".activity.MultiTypeActivity"
              android:label="MultiTypeActivity"
              android:parentActivityName=".activity.MainActivity"
        />
    <activity android:name=".activity.SingleSelectActivity"
              android:label="SingleSelectActivity"
              android:parentActivityName=".activity.MainActivity"
        />
    <activity android:name=".activity.MultiSelectActivity"
              android:label="MultiSelectActivity"
              android:parentActivityName=".activity.MainActivity"
        />
    <activity android:name=".activity.RecyclerViewNestedActivity"
              android:label="RecyclerViewNestedActivity"
              android:parentActivityName=".activity.MainActivity"
        />
  </application>

</manifest>
